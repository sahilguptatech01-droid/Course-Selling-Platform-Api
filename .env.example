# Database Connection
# Replace with your local database URL (PostgreSQL, MySQL, SQLite, etc.)
DATABASE_URL="postgresql://username:password@localhost:5432/course_selling_db?schema=public"

# Authentication
# Secret key used to sign and verify JWT tokens
JWT_SECRET="your_fallback_secure_jwt_secret_key_change_me_in_production"
